<h1><%= @tag.name %></h1>
<table>
  <thead>
    <tr>
      <th>Post</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.body %></td>
        </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', posts_path %>
